name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Cloudflare Pages auto-deploys on push
        run: echo "Cloudflare Pages is connected to this repo and deploys automatically on every push to main."
